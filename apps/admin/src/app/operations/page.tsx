import { CorridorDispatchRuntime } from '../../components/corridor-dispatch-runtime';

export const dynamic = 'force-dynamic';

export default async function OperationsPage() {
  return <CorridorDispatchRuntime />;
}
